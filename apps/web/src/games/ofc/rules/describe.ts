// Rule descriptions live in the engine so the web host screen and the dealer console share them.
export {
  variantName,
  variantLongName,
  rowLabel,
  fantasylandCardsText,
  multiplierText,
  describeRules,
  lowName,
} from '@bgf/ofc-engine';
